# Canvyst Hooks

> This directory will be moved to `@affine/worksapce/affine/hooks` in the future.

Only put hooks in this directory if they are specific to Canvyst, for example
if they are using the Canvyst API, or if the `AffineWorkspace` is required.
