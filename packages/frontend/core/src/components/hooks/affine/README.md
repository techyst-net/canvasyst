# Zeshan Hooks

> This directory will be moved to `@affine/worksapce/affine/hooks` in the future.

Only put hooks in this directory if they are specific to Zeshan, for example
if they are using the Zeshan API, or if the `AffineWorkspace` is required.
