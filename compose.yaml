# Generated by .brand/gen_repo_compose.py — regenerate rather than hand-edit.
#
#   infisical export --projectId techyst-13-affine --env prod --format dotenv > .env
#   docker compose up -d
#
# Images come from ghcr.io; `docker login ghcr.io` first. Databases, cache,
# queue and object storage are all external, so nothing here holds state and
# `docker compose down` is safe.

name: techyst-13-affine
services:
  affine_migration:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-affine-affine_migration:${TAG:-latest}
    env_file:
    - ./.env
    restart: 'no'
    pull_policy: always
    command:
    - sh
    - -c
    - node ./scripts/self-host-predeploy.js
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
  affine:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-affine-affine:${TAG:-latest}
    env_file:
    - ./.env
    restart: unless-stopped
    pull_policy: always
    ports:
    - 3040:3040
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: '3'
