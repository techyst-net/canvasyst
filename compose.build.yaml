# Generated by .brand/gen_repo_build.py — regenerate rather than hand-edit.
#
# Builds this app's images from the source in this directory. Used by ./build.sh;
# the server never needs this file, only compose.yaml.

name: techyst-13-affine
services:
  affine_migration:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-affine-affine_migration:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.techyst
      cache_from:
      - ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-affine-affine_migration:latest
  affine:
    image: ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-affine-affine:${TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.techyst
      cache_from:
      - ${REGISTRY:-docker.io}/${DOCKERHUB_USER:?set DOCKERHUB_USER}/techyst-affine-affine:latest
